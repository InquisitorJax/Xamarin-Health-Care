<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage x:Class="SampleApplication.Views.AppointmentPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Core.Controls;assembly=XForms.Shared"
             Title="{Binding Model.Name}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding SaveItemCommand}"
                     Order="Primary"
                     Priority="0">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource"
                            Android="ic_submit_dark.png"
                            WinPhone="Images/ic_submit_dark.png"
                            iOS="ic_submit.png" />
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid>
            <StackLayout Padding="{StaticResource FormPadding}">
                <controls:EntryEx EntryText="{Binding Model.Name}" LabelText="name" />
                <Label Text="Description" />
                <controls:EditorEx Text="{Binding Model.Description}" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>